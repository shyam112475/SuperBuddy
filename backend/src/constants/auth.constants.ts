export const BCRYPT_SALT_ROUNDS = 12;

/** How long a raw password-reset token stays valid. */
export const PASSWORD_RESET_TOKEN_TTL_MINUTES = 30;

/** Cookie name used to carry the refresh token. */
export const REFRESH_TOKEN_COOKIE_NAME = 'ch_refresh_token';

export const MIN_PASSWORD_LENGTH = 8;
