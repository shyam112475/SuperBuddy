# Production build configuration. Copy to .env.production and fill in real
# values before running `npm run build` for a production deploy — Vite
# inlines these at build time, so this file is never read at runtime.
VITE_API_URL=https://api.yourdomain.com/api
VITE_RAZORPAY_KEY_ID=
